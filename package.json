{
  "name": "dizzy-hackers-mega",
  "version": "1.0.0",
  "description": "High-fidelity hackathon landing page",
  "main": "index.html",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
